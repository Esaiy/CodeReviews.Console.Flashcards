namespace Flashcards.Esaiy.Enums;

enum Main
{
    ManageStack,
    ManageFlashcard,
    Study,
    ViewReport,
    Exit
};

enum StackMenu
{
    Create,
    GetAll,
    Pick,
    Update,
    Delete,
    Back
};

enum FlashcardMenu
{
    SelectStack,
    Create,
    GetAll,
    Update,
    Delete,
    Back
};

enum StudyMenu
{
    SelectStack,
    Start,
    GetAll,
    Back
};
